LLC.Collections.Revisions = Backbone.Collection.extend({
  // initialize: function(userId, noteId) {
  //   this.url = ('users/' + userId + '/notes/' + this.noteId +
  //   '/revisions/');
  // },
  model: LLC.Models.Revision
});